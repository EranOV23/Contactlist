Contacts List

In order to view the task you should run a local server. One options is:
To install: npm install http-server –g 
Run : http-server -a localhost -p 80
and navigate to "http://localhost:8080/#/" in your broswer.

This assignment was built with the concepet of a first mission in a larger project.
Therefore, things like separation views, multiple comments, setting variables etc. that may look
unnecessary in such a small project, could be beneficial in a larger one.